#!/usr/bin/env bash
#changes ip on localhost

cp /etc/hots ~/hosts.new
sed -i.bak 's/127.0.0.2/' ~/hosts.newsed 
sed -i.bak '/localhost/ a 8.8.8.8 facebook.com' ~/hosts.new
cp -f~/hosts.new /etc/hosts
